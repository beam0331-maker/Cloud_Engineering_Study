-------------------------------------------------
2026.05.

Serblet/JSP 강의

1. 자바 개발 플랫폼 3가지

	1) Java SE (Java Standard Edition)
		- 자바 spec
		- JDK (JVM, 개발툴(javac, java), API(.jar))
		- 시작점(starting point)인 main 메서드를 가짐.
	
	2) Java EE ( Java Enterprise Edition )
		- 웹 어플리케이션 개발을 플랫폼.
		- Servlet/JSP spec( 인터페이스 )
		- 실제 구현체를 제공하는 벤더는 Apache임
		  ( Tomcat 서버와 Servlet/JSP의 jar 제공 )
	
	3) Java ME ( Java Micro Edition )
	
2. 개발환경

	1) java SE 환경 구축
	
		- JDK 설치( JDK21 )
		- 환경변수 설정
			
			JAVA_HOME=
			PATH= 
			
	2) Eclipse 설치
		https://www.eclipse.org
		
		Eclipse IDE 2025-3 R Paakge 의 Eclipse IDE for Enterprise Java and Web Developers 다운
		
		
	3) Tomcat 서버 (Tomcat 컨테이너)
	 https://jakarta.apache.org
	 https://tomcat.apache.org/download-10.cgi 에서 Tomcat 10 다운로드
	 
	 // servlet / jsp 벤더가 Oracle에서 Apache로 넘어가면서 패키지 이름이 바뀜
	 // Tomcat 10 부터 버전 차이가 발생하니 유의해 둘것
	 # tomcat 9과 tomcat 10 차이점
	 
		tomcat 9 : JDK11 까지만 지원됨
					  Oracle 벤더에서 관리
					  패키지명은 javax.servlet 시작됨
		
		tomcat10 : JDK 17 이상 지원됨
					  Apache 벤더에서 관리
					  패키지명은 jakarta.servlet 시작됨
		https://tomcat.apache.org/tomcat-10.1-doc/servletapi/index.html
		
	 # 필요시 환경변수 설정필요
		CATALINA_HOM=C:\cloud_engineering\Boot_Camp_6th\07_Servlet_JSP\apache-tomcat-10.1.54
		
	4) 압축풀기 - 같은폴더에 풀기
		C:\cloud_engineering\Boot_Camp_6th\07_Servlet_JSP\apache-tomcat-10.1.54\bin
		C:\cloud_engineering\Boot_Camp_6th\07_Servlet_JSP\eclipse\configuration
		
	5) eclipse와 tomcat 연동
		
		# 서블릿/JSP 웹 어플리케이션 배포 방법
			
			가. eclipse에서 웹어플리케이션 생성
			나. 웹어플리케이션 압출 ( test.war )
			다. tomcat 서버에 배포
				 tomcat\webapps 에서 배포가능
				 
			라. tomcat이 자동으로 test.war 압축 해제
				tomcat\webapps\test : html, jsp, css, image 파일들이 위치한다. 
				.\WEB-INF // 필수. 반드시 대문자
				.\WEB-INF\lib 		: Java SE의 build Path와 동일한 기능.(  *.jar )
				.\WEB-INF\classes : 작성한 *.class 파일들이 위치하는 폴더
				.\classe\web.xml
			
			마. 웹브라우저에서 웹어플리케이션 요청
				http://localhost:포트번호/자원
				http://localhost:8080
				
	# 물리적인 이름(pc에 저장된 이름, web application)과 논리적인 이름(context)은 매핑되어 있다
	  => 브라우저에서 context를 호출하면 매핑된 web application을 찾아 불러온다.
	
3. 웹프로젝트 생성

	File > new > Dynamic Web Project 선택>
		
		Web Moudule 화면
			
			Context root: 논리적 이름(컨텍스트명), 수정가능
							  기본적으로 프로젝트명으로 설정됨.
							  http://localhost:8080/컨텍스트명 (*)
			Content Directory: src/main/webapp <== 이클립스가 관리하는 디렉토리
				
					Generate Deployment Dscriptor: web.xml ( 배치 지시자 ), 하나의 웹 어플리케이션 설정정보 관리.															 
															 
///////////////////////////////////////////////////////////////////////////////////

4. 웹 컴포넌트 작성 ( 클라이언트에서 )

	1) 서블릿
	2) jsp  ( html과 동일)
	
		3-step 작업
		가. 변환단계
			hello.jsp --------> hello_jsp.java
		나. 컴파일단계
			hello_jsp.java ---> hello_jsp.class
		다. 실행단계
			hello_jsp.class --> html 코드로 결과가 나옴
	
	3) html (사용안함, 대신 jsp 사용)

	프로젝트명
		src
			main
			|- java
			|- webapp
				|- META-INF
				|- WEB-INF		<== WEB-INF는 외부(웹 브라우저)에서 접근이 불가(보안)
				|- *.html
				|- *.jsp
				|- *.cs
				|- *.js
				|- 디렉토리
					|- *.html
					|- *.cs
					|- *.js

5. 서블릿(Servlet)

	1) 특징
		- *.java
		- 저장위치는 src/main/java에 저장
		- 반드시 패키지 필수 (*)
		- main 메서드가 없음. 시작점이 없다.
		  ( 사용자가 맨 처음 요청하는 서블릿이 시작점이 된다. 유저는 다양한 루트를 통해 접근할 수 있다. )
		  
		- 직접 new하지 않음.
			Tomcat 서버가 필요한 시점에 생성부터 소멸까지 관리해줌
			Tomcat 서버가 필요한 이유: Servlet/JSP 같은 동적컴포넌트를 관리함
			
		- 실행결과는 html로 나옴
	
	2) 구현 방법 (*)
		
		가. 패키지로 구성
			ex. com.servlet
			
		나. extends HttpServlet ( jakarta.servlet.http 패키지 )
			
			////////////////////////////////////////////////////
			 서블릿의 계층구조 (외우기 *)
				
				Servlet, ServletConfig ( 인터페이스 )				
					|
				GenericServlet ( 추상 클래스 )
					|
				HttpServlet ( 추상 클래스 )
					|
				사용자 서블릿
				
				Servlet 인터페이스: init(), service(), destroy(), getServletConfig()
				ServletConfig 인터페이스 : getInitParametor(), getServletName(), getServletContext()
				GenericServlet 추상클래스 : init(), service(), destroy(), getInitParametor()
				HttpServlet 추상클래스 : doGet(), doPost(), doPut(), doDelete()
				
				https://tomcat.apache.org/tomcat-10.1-doc/servletapi/index.html
												
				==> 사용자 서블릿에서 위의 메서드를 사용 또는 재정의가 가능 (*)
		
			////////////////////////////////////////////////////		
		
		다. doGet(기본),doPost : 반드시 재정의해서 사용해야된다.(*)		
			 ( html에서 method = "get(기본)| post" )	
		
		라. 서블릿맵핑
			
			ex. 
				src
				|-main
					|-java
						|-com
							|-servlet
								|-HelloServlet
			
			요청: http://localhost:8080/컨텍스트명/servlet/풀패키지의서블릿명
					http://localhost:8080/hello/servlet/com.servlet.Helloservlet  ==> 현재 사용 안됨
					
					- 보안취향, URL이 길다
					
			==> [servlet/com.servlet.Helloservlet] 부분을 별칭을 준다 ==> 서블릿 맵핑
				 http://localhost:8080/hello/서블릿맵핑
				 
			# 서블릿 맵핑 방법 2가지
				
				가. 어노테이션 이용
					- @WebServlet("/서블릿맵핑")	# / 필수
					  @WebServlet("*.do") 			# 확장자패턴
					  
				나. web.xml
				
				<!-- 서블릿 맵핑 -->
				<servlet>
						<!-- servlet-name 태그를 통해 servlet과 servlet-mapping 태그를 연결한다 -->
					  <servlet-name>WorldServlet</servlet-name>
					  <servlet-class>com.servlet.WorldServlet</servlet-class>
				 </servlet>    
				 <servlet-mapping>
					  <servlet-name>WorldServlet</servlet-name>
					  <url-pattern>/World</url-pattern>
				<servlet-mapping>
			
				==> Spring Framework에서 내부적으로 사용되는 서블릿매핑 방법 (DispatcherServlet, / 으로 매핑)

6. 서블릿 LifeCycle
	- Tomcat 컨테이너가 Servlet의 LifeCycle(생성-소멸) 관리함.
	  생성 때 호출되는 콜백메서드 (시스템이 호출하는 메서드) 제공 : init()
	  소멸 때 호출되는 콜백메서드 (시스템이 호출하는 메서드) 제공 : destroy()
	  요청 때 호출되는 콜백메서드 (시스템이 호출하는 메서드) 제공 : doGet/doPost
	  
	1) init() 메서드
		- 서블릿이 생성될 때 자동으로 호출됨.
		- 한번만 호출됨 ==> 서블릿은 한번만 생성된다.
		 
	2) doGet/doPost 메서드 ( 서비스 메서드라고 부름 )	
		- 서블릿에 요청할 때 자동으로 호출됨.
		- 요청할 때 마다 매번 실행됨. (*)
		
		코드 구현:
			- doGet(HttpServletRequest request, HttpServletResponse response){}
			
			- doPost(HttpServletRequest request, HttpServletResponse response){}
		
			==> HttpServletRequest와 HttpServletResponse는 로컬변수이다
				 따라서 요청할 때마다 매번 새롭게 생성된다.(*** 가장 중요) // [이유 작성하기]
	
	3) destroy() 메서드
		- 서블릿이 삭제될 때 호출 ( 서버측에서 파일을 수정하며 재시작될때 이전 서블릿이 소멸되면서 새로운 서블릿이 생성된다 )
		- 단 한번 호출
		
	==> 서블릿은 한번만 생성되고, 여러 사용자 요청을 하나의 서블릿이 처리한다.(*)
	
	- 서블릿에서 선언하는 변수는 서블릿의 주기와 같이 하기 때문에 불특정 다수에게 노출된다.
	- 특정인의 요청에만 변수가 노출되게 하고 싶다면 doGet() 에서 선언해야 한다.

-------------------------------------------------
2026.05.06

7. 서블릿의 thread-safe와 thread-safe

	1) 서블릿의 2가지 변수 사용시 공유하거나 공유하지 않는 개념
		- thread-safe: 변수 공유하는 상황
		- thread-safe: 변수 공유하지 않는 상황
		
	2) 인스턴스 변수
		- 서블릿은 단 한번만 생성되기 때문에 인스턴스 변수도 단 한번만 생성됨.
		  따라서 여러 사용자들ㅇ과 공유할 수 있기 때문에 안전하지 않음.
		  
	3) 로컬 변수
		- doGet/doPost 메서드내의 로컬변수는 요청할 때마다 실행되는 메서드이기 때문에
		  여러 사용자들과 공유되지 않음. 따라서 안전함.(*)
		  
8. 응답처리
	
					  요청(HttpServletRequest)
	웹브라우저 -----------------------------> 서버
				  <-----------------------------
					  응답(HttpServletResponse)
					  
	1) 개념	
		- 서블릿에서 html작성해서 웹브라우저에게 전달하는 작업의미.
		- HttpServletResponse API가 처리함
		
		
	2) 구현방법
		가. MIME타입 지정
			- 서버의 서블릿이 웹브라우저에게 전달되는 데이터 종류(MIME타입)를 알려주는 작업의미.
			
			문법:
				response.setContentType("MIME타입지정");
				
				response.setContentType("text/html"); // text/plain (일반텍스트)
																	  text/html:utf-8 : 한글깨질때 사용
				
		나. 출력시 사용하는 API 이용
			
			문법: PrintWriter out = response.getWriter();
		
		다. html 출력
			
			문법: 
				out.print("<html>");
				...
				out.print("</html>");
				
		===> 서블릿은 대부분이 자바 코드(비즈니스로직 처리)이고 일부분이 화면처리 (응답처리) 코드가 포함됨
			  문제는 화면처리(응담처리)를 잘 못함. (*)
			  나중에 화면처리를 잘하는 컴포넌트(JSP)에게 일임 한다.
				
				
9. 요청처리 (*)

					  요청(HttpServletRequest)
	웹브라우저 -----------------------------> 서버
				  <-----------------------------
					  응답(HttpServletResponse)
					  
	1) 개념
		- 웹브라우저에서 사용자 입력폼에 값을 입력하고 서블릿에 전달해서 서블릿이 값을 조회하는 작업의미.
		- HttpServletRequest API가 처리함.
		
	2) 사용자 입력 데이터를 가져오는 메서드
		 
		 ex.
			<form action="타겟(서블릿의 메핑값)" method="GET|POST">
				이름<input type="text" name="username">
				나이<input type="text" name="age">
				<input type="submit" value="전송">
			</form> 
		
		 # form의 데이터는 JSON형태로 전달되며 이때 key는 name 이다. 
		 get 요청: 
			http://localhost:8080/컨텍스트명/서블릿매핑?key=value&key=value
			
			http://localhost:8080/컨텍스트명/서블릿매핑?username=value&age=value
			
 		 post 요청: 
			http://localhost:8080/컨텍스트명/서블릿매핑
			body: key=value&key=value
					username=value&age=value
				
		String s = request.getParameter("html 태그의 name값");
		// 사용자가 입력한 모든 값은 문자열로 서블릿에 전달됨(*)
	
		String [] s2 = request.getParameterValues("html 태그의 name값");
		// html의 체크박스같은 경우
		
		Enumeration<String> enu = request.getParameterNames();	
		// Map의 keySet() 기능
		// Enumeration(hasMoreElements(), nextElement())은 
			Iterator(hasNext(), next()) 역할
			
			while(enu.hasMoreElements()){
				String key = enu.nextElement();
				String value = request.getParameter("key");
				
			}

10. scope (*** 가장중요)
	1) 개념
		- 자바의 변수( 로컬, 인스턴스, static )처럼
		  tomcat 컨테이너가 자동으로 데이터를 관리(저장, 조회, 수정, 삭제)하는 3가지 객체의 lifecycle 의미함.
		  
	2) 종류
	
		가. request scopre (HttpServletRequest)
				
			- 저장: request.setAttribute(String key, Object obj);
				
			- 조회: Object obj = request.getAttribute(String key);
									
			- 삭제: request.removeAtttibute(String key);
			
			# scope(Lifecycle): 요청-응답
				- doGet / doPost 메서드 안에서만 사용이 가능함(*)
				- 용도: 게시판 목록보기, 
					
		나. session scope (HttpSession)
			
			# session(저장소)이 있으면 리턴, 없으면 새로생성해서 리턴			
			HttpSession session = request.getsession();
			HttpSession session = request.getsession(true);
			------------------------------------------------
			
			# session(저장소)이 있으면 리턴, 없으면 null 리턴			
			HttpSession session = request.getsession(false);
			
			- 저장: session.setAttribute(String key, Object obj);
				
			- 조회: Object obj = session.getAttribute(String key);
									
			- 삭제: session.removeAtttibute(String key);
			
			# scope(Lifecycle): 웹브라우저와 scope가 동일 (동일 웹브라우저 기준: 크롬 1개, 엣지 1개)
				- 브라우저(크롬,엣지,...)당 1개의 세션 메모리 할당. 크롬을 두개 켜도 한개의 세션 메모리를 공유
				- 이론적으론 브라우저 닫기 전까지 세션 메모리는 유지됨
				- 보안 이슈로 timeout(30분)이 지정되어있다.
					//////////////////////////////////////////////////////////					
 					<session-config>
						  <session-timeout>30</session-timeout>
					</session-config>
					
					session.setMaxInactiveInterval(int interval) // 초단위
					//////////////////////////////////////////////////////////
				- 웹브라우저에서 서블릿에 요청하면 Tomcat 서버에서 해당 브라우저의 세션 메모리가 할당된다.
				- 세션 메모리는 JSON 구조이다.				
				- 서블릿에서 세션 메모리를 불러온다
					: HttpSession session = request.getsession();
				
				- 세션 저장
					ex. session.setAttribute("a","b"); ===> 세션 메모리에 "a":"b" 형태로 저장
					
				- 응답: 웹브라우저에서 렌더링. ===> 세션 메모리 유지중				
				- 소멸: 웹브라우저 창을 닫으면 세션 메모리가 삭제된다.
			
			- 용도: 로그인, 장바구니, 
				
		다. application scope ( ServletContext )
			
			ServletContext ctx = getServletContext();	// ServletConfig 인터페이스 메서드
			
			- 저장: ctx.setAttribute(String key, Object obj);
				
			- 조회: Object obj = ctx.getAttribute(String key);
									
			- 삭제: ctx.removeAtttibute(String key);
			
			# scope(Lifecycle): tomcat 컨테이너와 scope가 동일	//생명주기가 가장 길다
				- tomcat 컨테이너를 종료하지 않으면 데이터를 계속 사용가능
				
			- 용도: 블로그 방문자 수 
			
11. 필터(Fileter)
								 필터1 필터2
					  요청	  //	  //
	웹브라우저 ------------//----//--------> 서버
				  <-----------//----//---------
					  응답	  //	  //
					  
	1) 개념:
		- 서블릿에 요청하기전 및 응답 후에 특별한 작업처리가 가능.
		- 선처리 및 후처리 가능
		- 필터는 1개 이상 적용이 가능하다 => 중복가능
		
	2) 구현 방법
	
		가. 패키지로 작성
		나. implments Filter
		다. doFilter() 메서드 재정의
		라. 필터매핑
			- @WebFilter("/매핑")
			ex.
				@WebFilter("/서블릿매핑값")  <== 지정된 서블릿을 요청했을 경우에만 필터 실행됨
				@WebFilter("/*") 				  <== 임의의 서블릿을 요ㅇ했을 경우 필터 실행됨
			- web.xml 이용
			ex.
				<filter>
					<filter-name>MyFilter</filter-name>
					<filter-class>com.filter.MyFilter</filter-class>
				</filter>
				<filter-mapping>
					<filter-name>MyFilter</filter-name>
					<url-pattern>/*</url-pattern>
				</filter-mapping>
		
		==> Spring에서 보안관련 서브프로젝트인 Spring Security에서 사용됨.
		
12. DB 연동( MyBatis 이용 )

	1) 2개의 jar 필요
		mybatis-3.5.17, mysql-connector-j-8.0.33.jar
		
	2) com/config/jdbc.propertise
			jdbc.dirver=com.mysql.cj.jdbc.Driver
			jdbc.url=jdbc:mysql://localhost:3306/testdb
			jdbc.userid=root
			jdbc.passwd=1234			

   3) com/dto/DeptDTO.java 작성

   4) 2개의 xml 작성

     가. com/config/configuration.xml

     나. com/config/DeptMapper.xml

   5) com.config.MySqlSessionFactory.java 작성
	 
	6) layer 작성
	
										DeptServiceImpl
	브라우저 ------------>		DeptService					DeptDAO					mysql
-------------------------------------------------
2026.05.07

1. 요청 위임

	1) 개념
	
		가. 현재상태
			
								요청
						 ----------------------------------> 
			웹브라우저 												서블릿 (비즈니스로직 + 화면처리로직)
						 <----------------------------------
								응답( 서블릿은 html작성에 적합하지 않다 )
								
	2). 요청위임
		
								요청
			웹브라우저 ----------------------------------> 서블릿 (비즈니스로직)
																			|		
																			| 요청위임
																			| : forward 방식, redirect 방식
																			|
						  <---------------------------------- JSP (화면처리 로직) - 서블릿, html 가능
								응답
								
			- 서블릿이 jsp를 선택하는 작업, 
			  웹브라우저에서 서블릿에게 url입력해서 요청하는 방식과 동일한 방식으로 요청해야 한다.
				==> 서블릿이 jsp에 요청을 위힘하는 작업은 웹브라우저가 jsp에 요청하는 것과 같은 의미를 가진다
			
		가. forward 방식
			- HttpServletRrequest 사용
			
			- 문법:
				RequestDispatche dispatcher = request.getRequestDispatcher("target");
				dispatcher.forward(request, response);
				
				# 한번에 사용
				request.getRequestDispatcher("target").forward(request, response);
				
				ex.
					request.getRequestDispatcher("target.jsp").forward(request, response);
			
			-특징: URL이 변경이 안됨.
			
							1) 요청( http://locahhost:8080/app8/RedirectServlet )
							HttpServletRequest 하고 HttpServletResponse 생성됨
			웹브라우저 ----------------------------------> 서블릿 (비즈니스로직 )
																		  request.getRequestDispatcher("target2.jsp").forward(request, response);
																			|
																			|	2) 서블릿이 웹브라우저처럼 재요청
																			|		"1) 요청"에서 생성된 request를 재사용한다.
																			|
							<--------------------------------- target2.jsp ( 화면처리)
							3) 응답
							
			- 동일한 request를 재활용하기 때문에 request scope 수준의 데이터도 서블릿-jsp에서 공유가능
								
		나. redirect 방식
			- HttpServletResponse 사용
			
			- 문법:
				
				response.sendRedirect("target");
				
				ex.
					response.sendRedirect("target.jsp");
					// http://locaalhost:8080/app/target.jsp 와 같은 요청이다 (웹브라우저의 요청 방식과 동일)
					
			- 특징:
				URL이 변경된다
				
								1) 요청( http://locahhost:8080/app8/RedirectServlet )
								HttpServletRequest 하고 HttpServletResponse 생성됨
				웹브라우저 ----------------------------------> 서블릿 (비즈니스로직 )
																				2)요청위임:
																				request.setAttribute("key", "홍길동");
								<----------------------------------	response.sendRedirect("target.jsp");
								3) 응답("target.jsp 정보 포함")
								
								4) 요청( http://locahhost:8080/app8/RedirectServlet )
								HttpServletRequest 하고 HttpServletResponse 생성됨
								---------------------------------->	 target.jsp( 화면처리 )
								<----------------------------------  request.getAttribute("key"); // null
								5) 응답
				
				- [1) 요청]과 [4) 요청]에서 생성되는 HttpServletRequest, HttpServletResponse가 서로다름(**)
				- servlet - jsp 간의 데이터 전달은 session scope에서 이루어져야 한다.
				  (request scope는 서로 다른 요청이므로 서로의 데이터 접근이 불가능)
				  
			- 용도: 홈페이지, 로그인,
			
2. 세션 관리 ( Session tracking )
	
	1) 개념
		
		- 현재 사용중인 프로토콜: http.
		- http 프로토콜의 주요 특징: Connectionless, Stateless
		// Connectionless: 요청-응답 처리가 끝나면 http는 클라이언트와 서버의 연결을 유지시켜주는 파이프를 제거한다
								==> 지속적인 연결은 차단한다.
		
								요청
			웹브라우저 ----------------------------------> 서블릿
						  <----------------------------------
								응답
		
			따라서 이전 페이지에서 했던 작업들을 현재페이지에서 인식이 불가능함(*)
			실제 웹어플리케이션에서는 현재페이지가 이전페이지를 인식한다.
			==> 세션관리를 통해 가능하게 된다. 즉 서블릿 및 jsp 간의 데이터를 공유시키는 작업
			
	2) 방법 2가지 
	
		가. 세션이용
			- HttpSession 이용
			- 여러 서블릿 및 jsp 간의 데이터를 공유하기 위해서는 공유데이터를 tomcat 서버에 저장하는 방식
								
			- 용도: 로그인
			- 아키텍쳐
	///////////////////////////////////////////////////////////////////////////////////////////////////////////////
																					tomcat ( 서버 )		
																					2) AAA 저장소(세션) 생성: key | value
																													 -----|--------
																								4)	데이터저장 "id"	| "aaaa"
				  1) 요청: 
				  웹브라우저의 id 값이 전달됨, sessionid = AAA								
	웹브라우저 --------------------------------------------> 3) 서블릿1에서 세션접근
	(AAA)																		# 세션이 있으면 반환하고 없으면 새로 생성해서 반환
																				HttpSession session = request.getSession();
																				HttpSession session = request.getSession(true);
																				----------------------------------------------
																				# 세션이 잆으면 반환한고 없으면 null 반환
																				HttpSession session = request.getSession(false);
																				
																				* 저장하는 서블릿코드(ex. 로그인)
																				4) 필요한 정보를 세션에 저장
																				HttpSession session = request.getSession();
					<-------------------------------------------	session.setAttribute("id","aaaa");
	6) 랜더링	5) 응답																
	7) 연결 끊음																				
					8) 요청: 
					웹브라우저의 id 값이 전달됨, sessionid = AAA
					--------------------------------------------> 9) 서블릿2에서 세션 접근
																				 HttpSession session = request.getSession();
																				 // 서블릿1이 실행되었다면, AAA id로 만들어진 세션이 존재한다.
																				 // 서블릿1을 거쳤는지 보장할 수없음
																				 // ==> 데이터를 얻을때 null을 확인해야한다
																				 
																				 10) 데이터 얻기
																				 # 데이터 단위에서 확인 ( 주로 사용 )
																				 Strint data = (String)sesseion.getAttribute("id");
																				 if(data != null){
																					// 필요한 데이터 사용
																				 } else{
																					// 서블릿1을 먼저 실행하도록 유도
																					response.sendRedirect("서블릿1")';
																				 }
																				 
																				 # 세션 단위에서 확인
																				 HttpSession session = request.getSession(false);
																				 if(session != null){
																					Strint data = (String)sesseion.getAttribute("id");
																				 } else{
																					// 서블릿1을 먼저 실행하도록 유도
																					response.sendRedirect("서블릿1")';
																				 }
																				 
		# 세션 데이터 삭제
			AAA 저장소(세션): key | value
								  -----|--------
					           "id" | "aaaa"
								"cart" | new ArrayList(...)
								
			- 세션 메모리 삭제: session.invalidate();		ex. 로그아웃
			- 특정 엔트리(행) 삭제: session.removeAttribute("key");		ex. 장바구니 삭제.									  
																				 
	///////////////////////////////////////////////////////////////////////////////////////////////////////////////
			
			
			
		나. 쿠키이용
			- 특징: 클라이언트에 저장됨.
					  value는 String만 저장됨
					  도메인당 3000, cookie는 4kb 까지 허용됨
				
			- Cookie 이용
			- 여러 서블릿 및 jsp 간의 데이터를 공유하기 위해서는 공유데이터를 클라이언트에 저장하는 방식
			
			웹브라우저 ----------------------------------> 서블릿1
																		  서블릿2	
		공유데이터 저장											  jsp
						  <----------------------------------	
			
			- 용도: 고객 CRM
			- 아키텍쳐			
	///////////////////////////////////////////////////////////////////////////////////////////////////////////////		

				  1) 요청			 	
	웹브라우저 --------------------------------------------> 서블릿1
																				# 로직처리 후 공유데이터를 쿠키저장
																				
																				2) 쿠키 생성
																				// value는 문자열만 가능함
																				Cookie c = new Cookie(key, String value);
																				
																				# 쿠키 time-out 지정가능
																				c.setMaxAge(초);
																				양수 값: 파일 저장
																				음수 값: 웹브라우저에 저장 (브라우저 종료시 쿠키 값이 삭제됨)
																				0 		 : 쿠기 즉시 삭제 (session.invalidate() 와 동일 )
																				
					3) 응답: 쿠키전달										3) 클라이언트에 전달
				  <--------------------------------------------	response.addCookie(c);
	4) 클라이언트에 저장
	- 웹브라우저에 저장(휘발성)
	- 물리적인파일로 저장(비휘발성)
				  
				  5) 요청: 쿠키 전달
				  --------------------------------------------> 서블릿2
				  
																				6) 쿠키 얻기
																				Cookie[] cookies = request.Cookies();
																				
																				for(Cookie c : cookies){
																					String key = c.getname();
																					if("xxx".equals(kdy)){
																						String value = c.getvalue;
																					}
																				}
				  ... 99) 쿠키 삭제 요청
				  --------------------------------------------> 100) 쿠키 삭제
																				Cookie[] cookies = request.Cookies();
																				
																				for(Cookie c : cookies){
																					String key = c.getname();
																					if("xxx".equals(kdy)){
																						// 쿠키 삭제
																						c.setMaxAge(0);
																						response.addCookie(c); // 클라이언트 한테 쿠크삭제 전달
																					}
																				}
				  <--------------------------------------------
					101) 응답
	102) 쿠키삭제
	
		# 세션과 쿠키 비교 정리
			
			가. 세션
				- 서버에 데이터를 저장
				- 저장되는 데이터 종류는 모든 데이터 가능(Object)
				- 서버에 저장되기 때문에 보안 이슈가 거의 없음.
				- 서버에 저장되기 때문에 데이터 저장 크기 이슈가 없음.
					( 성능이 떨어지는 서버인 경우에는 이슈가 있음 )
				
			나. 쿠키
				- 클라이언트에 데이터를 저장
				- 저장되는 데이터 종류는 문자열만 가능 ( String )				
				- 클라이언트에 저장되기 때문에 보안 이슈가 있음.
				- 클라이언트에 저장되기 때문에 데이터 저장 크기 이슈가 없음. ( 과점에 따라서 달라짐 )
				- 사용자가 쿠키 사용을 비활성화 시킬 수 있음. ( 매우 큰 리스크 )
				- 도메인 당 3000개까지 생성가능. 크기 4kb
			
			실무에서는 세션과 쿠키를 같이 사용함
			
	///////////////////////////////////////////////////////////////////////////////////////////////////////////////

3. JSP ( Java Servet Page )

	1) 특징
		- 서블릿처러 동적 컴포넌트
		- *.jsp
		- 3단계로 실행됨.
			가. 변환단계
				test.jsp ----------> test_jsp.java
			나. 컴파일단계.
				test_jsp.java ----------> test_jsp.class
			다. 실행단계
				test_jsp.class ----------> html
				
	2) 구성요소
		
		가. html 태그
		나. JSP 태그
		
			- directive 태그
				* page directive		: <%@ page 속성명="속성값" ... %>
				* include directive	: <%@ include page 속성명="속성값" ... %>
				* taglib directive	: <%@ taglib page 속성명="속성값" ... %>
			
			- declaration 태그: <%! 자바코드 %> ( 서블릿의 인스턴스변수,  메서드 역할의 코드지정 자주 사용하지 않는다 )
			- scriptlet 태그	: <% 자바 코드 %> ( 서블릿의 doGet 역할의 코드 지정 )
			- expression 태그	: <%= 변수 %> ( 서블릿의 응답처리 역할의 코드 지정. 즉 웹브라우저에 출력됨 )
			
		나2: jsp액션태그
		
         <jsp:프리픽스  속성명="" />

			 ex. <jsp:include>
				  <jsp:param>
				  .. 
		
		다. EL ( Expression Language )
			
			문법: ${표현식}
			
		마. JSTL ( JSP Standard Tag Library )
		
			- 커스텀 태그 
				ex. 반복, 조건, ... 
					
					<c:if test=""></c:if>
					
4. 웹 어플리케이션을 개발하기 위한 방법 2가지
	
	1) Model 1 Architecture
		- 순수하게 모든 작업을 JSP로 처리하는 방식
		ex. 
			웹브라우저 <----> JSP ----> service ----> DAO ----> MySQL
		
		특징:
			- 짧은 시간안에 웹 어플리케이션 개발이 가능.
			- 관리에 많은 작업이 필요하다.
			- 현재는 사용안함. 
			
	2) Model 2 Architecture( MVC: Model View Controller ) (***)
		
		- 서블릿과 JSP 같이 사용하는 방식
		ex. 			
														Model(Data) - data처리
								Controller		|---------------------------|
			웹브라우저 ----> 서블릿 ----> service ----> DAO ----> MySQL
									|
									| 요청 위임
									|
						  <---- JSP(View) - 화면 구현
			
		- MVC(Model View Controller) 구조이다.
		- 서블릿은 Model과 View를 관리한다.
		- JSP가 클라이언트의 요청을 받을 순 있지만 항상 서블릿이 요청받게 작성해야 한다.
-------------------------------------------------
2026.05.08

1. directive 태그 ( 지시자 태그 )
		
	가. page directive
		- 용도: JSP에게 특정 정보를 알려줄때 사용.
		- 문법: 
			<%@ page 속성명 = "속성값" ... %>
			
			<%@ page contentType = "text/html" %>	==> 서블릿의 response.setContentType("text/html")와 동일
			
			<%@ page import="java.util.ArrayList" %>
			<%@ page import="java.util.Date" %>
			
			<%
				ArrayaList list = new ArrayList();
				Date = new Date();
			%>			
	
	나. include directive
		- 용도: 화면 재사용, HTML의 <iframe> 기능과 동일
		- 문법:
			<%@ include file="jsp파일" %>
			
			특징: *.java 변환되기 전에 포함시킴
			
		- 문법2:
			<jsp:include page="jsp파일" />
			
			특징: 포함시킬 jsp에 요청을 하고 실행된 결과를 포함시킴
			
	다. taglib directive
	
		- 용도: JSTL에서 사용됨
		- 문법: 
			<%@ taglib preifx="" url="" %>
			
2. declaration 태그 ( 거의 사용안함 )
	
	- 용도: 서블릿의 인스턴스 변수 및 메서드 (doGET 제외)
	- 문법: 
		<%! 
			인스턴스 변수
			메서드
			...
		%>
		
3. scriptlet 태그 ( 가장 많이 사용)

	- 용도: 서블릿의 doGet 메서드 내에서의 작업
	- 문법: 
		<%
			자바코드(메서드 사용 불가)
		%>

4. expression 태그

	- 용도: 웹 브라우저에 값 출력
	- 문법: 
		<%= 표현식 %>
		<%= 변수 %>
		<%= 변수.메서드() %>
		
5. 내장객체 (내장변수) (외우기)

	1) 개념:
		jsp가 *.java로 변경될 때, 항상 만들어지는 변수 의미함.
		이 변수는 <% %> 에서 변수 선언ㅇ벗이 사용 가능함.
	
	2) 종류
		- reqeust	( HttpServletRequest )
		- response	( HttpServletResponse )
		- session	( HttpSession )
		- application	( ServletContext )
		- config			( ServletConfig )
		- out				( PrintWriter )
		
6. JSP 주석
 	<%-- jsp 주석--%>
	//	<!-- HTML 주석 -->
	
7. 게시판( MVC 모델 )

	1) Mode 2 Architecture
								 ( Controller )		(      				Modedl			        )	
		웹브라우저 ----------> 서블릿----------> 서비스 ----------> DAO ----------> mysql
										 | 
										 | 요청위임
										 |
					 <----------	JSP ( View ) 
					 
	2) 게시판 테이블
	
		create table board
		(  num int primary key auto_increment,
       title varchar(100) not null,
       author varchar(50) not null,
       content varchar(2000) not null,
       writeday date  default ( current_date ),
       readcnt  int default 0
    ); 
	 
		insert into board( title, author, content)
		values ('테스트', '홍길동','테스트입니다');
		commit;
		
	3) 목록보기																					
																		BoardserviceImpl			BoardDTO	
	웹브라우저 ----------> BoardListServlet----------> Boardservice ---------> BoardDAO ----------> mysql
									 | 
									 | 요청위임
									 |
				  <----------	list.jsp ( View ) 
	
	4) 글작성_화면보기
	
	웹브라우저 ----------> BoardWriteUIServlet
									 | 
									 | 요청위임
									 |
				  <----------	write.jsp ( View ) 
	4)글작성_동작
	
	웹브라우저 ----------> BoardWriteServlet
								 | 
								 | 요청위임
								 |
				<----------	BoardListservlet
				 
	5) 글 자세히 보기
																			BoardserviceImpl			  BoardDTO
	웹브라우저 ----------> BoardRetrieveServlet---------> Boardservice ----------> BoardDAO ----------> mysql
									 | 
									 | 요청위임
									 |
				 <----------	retrieve ( View ) 
	
	6) 글 수정하기	
																		 BoardserviceImpl			  BoardDTO
	웹브라우저 ----------> BoardUpdateServlet---------> Boardservice ----------> BoardDAO ----------> mysql
									 | 
									 | 요청위임
									 |
				  <---------- BoardListServlet
				  
	7) 글 삭제하기	
																		 BoardserviceImpl			  BoardDTO
	웹브라우저 ----------> BoardDeleteServlet---------> Boardservice ----------> BoardDAO ----------> mysql
									 | 
									 | 요청위임
									 |
				  <---------- BoardListServlet