server:
  port: ${SERVER_PORT:8080}

spring:
  application:
     name: ${APP_NAME:env-demo}
  profiles:
     active: ${SPRING_PROFILES_ACTIVE:local}
app:
  message: ${APP_MESSAGE:"기본메시지"}
  upload-dir: ${APP_UPLOAD_DIR:/app/upload}
